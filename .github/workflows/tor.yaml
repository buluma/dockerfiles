name: gh-pages Image CI

on:
  push:
    paths:
      - 'tor/Dockerfile'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file tor/Dockerfile --tag docker-test:$(date +%s)
