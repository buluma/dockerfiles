name: Vagrant Image CI

on:
  push:
    paths:
      - 'vagrant/Dockerfile'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file vagrant/Dockerfile --tag docker-test:$(date +%s)
