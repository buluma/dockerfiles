---
name: Confluence

on:
  push:
    paths:
      - 'confluence/**'
      - '.github/workflows/confluence.yaml'

jobs:
  yamllint:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: yamllint
        uses: ibiqlik/action-yamllint@v3

      - name: test dir
        run: ls -a confluence/

  build:

    runs-on: ubuntu-latest
    needs: yamllint

    steps:
    - uses: actions/checkout@v2
    - name: test dir
      run: ls -a confluence/

    - name: Run in Dir above
      run: cd confluence && ls -a

    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag docker-confluence-test:$(date +%s)
